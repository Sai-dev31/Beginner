#include <iostream>
using namespace std;

int main() {
    char Character;
    cout<<"Enter the Character for which ASCII Value should be shown: ";
    cin>>Character;

    cout<<"The ASCII Value of the character "<<Character<<" is "<<int(Character);

    return 0;
}